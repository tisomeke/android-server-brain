# 🧠 Android Server Brain (ASB)

**Превратите ваш Android-смартфон в мощный автономный сервер, управляемый через Telegram.**

> [!TIP]
> [🌐 *Read in English*](README.md)

### 📌 О проекте

ASB — это легковесный фреймворк на языке Go, оптимизированный для работы в среде **Termux**. Проект позволяет превратить неиспользуемый смартфон в полноценный серверный узел, доступный из любой точки мира через Telegram-бота.

### 🚀 Основной функционал

* **Мониторинг системы:** Получение данных о заряде батареи, свободном месте и аптайме через `termux-api`.
* **Удаленный Shell:** Выполнение любых Bash-команд прямо из чата с защитой от зависания (тайм-аут 30 сек).
* **Менеджер файлов:** Прием файлов через Telegram, сохранение в `~/downloads/server` и автоматическое создание симлинка в `~/server` с правами на исполнение (`chmod +x`).
* **Безопасная сеть:** Поддержка Tailscale для доступа к серверу без "белого" IP.
* **Безопасность:** Доступ разрешен только для AdminID, указанного в конфиге.

### 🛠 Требования и зависимости

Перед установкой ASB убедитесь, что у вас установлены следующие компоненты:

**Необходимые приложения (из F-Droid):**
* **[Termux](https://f-droid.org/packages/com.termux/)** - Эмулятор терминала и среда Linux
* **[Termux:Boot](https://f-droid.org/packages/com.termux.boot/)** - Автозапуск сервисов при загрузке
* **[Termux:API](https://f-droid.org/packages/com.termux.api/)** - Системные API для батареи, датчиков и т.д.

**Дополнительные требования:**
* Устройство на Android (API 21+)
* Стабильное интернет-соединение
* Аккаунт Telegram
* Токен бота от [@BotFather](https://t.me/BotFather)

### 📦 Установка

**Вариант 1: Автоматическая установка (Рекомендуется)**

Просто запустите установочный скрипт:
```bash
git clone https://github.com/yourusername/android-server-brain.git
cd android-server-brain
chmod +x install.sh
./install.sh
```

Скрипт автоматически выполнит:
- **Проверку системных зависимостей** (pkg, curl, ping)
- **Проверку статуса Termux:Boot** (требуется первоначальная настройка)
- Проверку и запрос разрешений на хранилище
- Установку необходимых зависимостей (golang, git, termux-api)
- Интерактивную настройку параметров бота
- Настройку директорий хранения и символических ссылок
- Сборку бинарного файла ASB
- Настройку **расширенной службы автозапуска** с обнаружением сети

**Вариант 2: Ручная установка**

1. **Установите пакеты Termux:**
   ```bash
   pkg update && pkg upgrade
   pkg install golang git termux-api
   ```

2. **Клонируйте и соберите ASB:**
   ```bash
   git clone https://github.com/yourusername/android-server-brain.git
   cd android-server-brain
   go build -o asb .
   ```

3. **Настройте приложение:**
   ```bash
   cp config.json.example config.json
   # Отредактируйте config.json с вашим токеном бота и ID администратора
   nano config.json
   ```

4. **Настройте автозапуск через Termux:Boot:**
   ```bash
   mkdir -p ~/.termux/boot
   echo '#!/data/data/com.termux/files/usr/bin/sh' > ~/.termux/boot/asb
   echo 'cd ~/android-server-brain && ./asb' >> ~/.termux/boot/asb
   chmod +x ~/.termux/boot/asb
   ```

### ▶️ Развертывание

**Ручной запуск:**
```bash
./asb
```

**Автозапуск:**
После настройки Termux:Boot, ASB будет запускаться автоматически при включении устройства с улучшенной надежностью:
- Обнаружение сетевого подключения
- Подробное логирование загрузки (`asb-boot.log`)
- Мониторинг процессов и защита от таймаутов
- Механизмы автоматического повтора

**Фоновый сервис:**
```bash
nohup ./asb > asb.log 2>&1 &
```

### 🗑 Удаление

Для полного удаления ASB из вашей системы:

```bash
./uninstall.sh
```

Скрипт удаления выполнит:
- Остановку всех запущенных процессов ASB
- Удаление бинарного файла и конфигурационных файлов
- Очистку скриптов автозапуска
- Удаление директорий хранения (по выбору)
- Подтверждение перед деструктивными действиями

### 📖 Использование

#### Команды Telegram:

* `/start` - Приветственное сообщение и базовая информация
* `/status` - Просмотр состояния системы (батарея, память, аптайм)
* `/battery` - Подробная информация о состоянии батареи
* `/watchdog` - Состояние службы мониторинга батареи
* `/exec <команда>` - Удаленное выполнение shell-команд

#### Управление файлами:

* **Загрузка файлов** - Отправьте любой файл боту
* Файлы сохраняются в `~/downloads/server`
* Автоматически создаются символические ссылки в `~/server` с правами на выполнение
* Доступ через `~/server/имяфайла` или прямой путь

#### Примеры использования:

```bash
# Проверка состояния системы
/status

# Выполнение системных команд
/exec ps aux
/exec df -h
/exec top -n 1

# Загрузка и запуск скриптов
# 1. Отправьте файл скрипта боту
# 2. Скрипт автоматически становится исполняемым
# 3. Запустите: /exec ~/server/myscript.sh
```

### 🏗 Структура проекта

```
android-server-brain/
├── config/
│   └── config.go          # Загрузка и валидация конфигурации
├── internal/
│   ├── bot/
│   │   └── router.go      # Обработчики команд Telegram-бота
│   ├── storage/
│   │   └── files.go       # Загрузка и управление файлами
│   └── system/
│       ├── monitor.go     # Мониторинг состояния системы
│       ├── shell.go       # Выполнение команд
│       └── watchdog.go    # Служба мониторинга батареи
├── main.go                # Точка входа в приложение
├── config.json           # Конфигурационный файл
├── install.sh            # Скрипт автоматической установки
└── go.mod               # Зависимости Go-модуля
```

### ⚙️ Конфигурация

Создайте `config.json` со следующей структурой:

```json
{
  "telegram_token": "ВАШ_ТОКЕН_БОТА_ЗДЕСЬ",
  "admin_id": 123456789,
  "storage_dir": "downloads/server"
}
```

* `telegram_token`: Получите у [@BotFather](https://t.me/BotFather)
* `admin_id`: Ваш ID пользователя Telegram (используйте [@userinfobot](https://t.me/userinfobot) для его получения)
* `storage_dir`: Директория для загружаемых файлов (относительно домашней директории)

### 🔒 Замечания по безопасности

* Только указанный AdminID может управлять сервером
* Все команды выполняются с правами пользователя Termux
* Загружаемые файлы проверяются и хранятся в изолированной директории
* Доступ к сети зависит от ваших настроек безопасности Telegram

### 🐛 Решение проблем

**Частые проблемы:**

1. **Бот не отвечает:**
   - Проверьте интернет-соединение
   - Убедитесь в правильности токена бота в config.json
   - Проверьте корректность AdminID

2. **Команды не выполняются:**
   - Проверьте разрешения Termux API
   - Убедитесь, что все необходимые пакеты установлены
   - Просмотрите логи в `asb.log`

3. **Автозапуск не работает:**
   - Предоставьте разрешение Termux:Boot в настройках Android
   - Проверьте права доступа к скрипту `~/.termux/boot/asb`
   - Протестируйте ручное выполнение скрипта автозапуска

**Просмотр логов:**
```bash
# Основной лог приложения
tail -f asb.log

# Лог последовательности загрузки (для проблем с автозапуском)
cat asb-boot.log
```

### 🤝 Вклад в развитие

Вы можете отправлять issues, запросы на функции или pull request'ы. Для крупных изменений сначала откройте issue для обсуждения предложенных изменений.

### 📄 Лицензия

Этот проект лицензирован по лицензии GNU General Public License v3.0 - подробности смотрите в файле LICENSE.
